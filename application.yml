global:
 message: welcome to global microservices environment!!
    logging:
    level:
    root: Info
   managements:
   endpoints:
   web:
    exposure:
    include: health,info,refresh
    endpoint:
    health:
    show-details: always
    
     
    
  
